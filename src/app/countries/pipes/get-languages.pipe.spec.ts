import { GetLanguagesPipe } from './get-languages.pipe';

describe('GetLanguagesPipe', () => {
  it('create an instance', () => {
    const pipe = new GetLanguagesPipe();
    expect(pipe).toBeTruthy();
  });
});
