import { GetCapitalPipe } from './get-capital.pipe';

describe('GetCapitalPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCapitalPipe();
    expect(pipe).toBeTruthy();
  });
});
